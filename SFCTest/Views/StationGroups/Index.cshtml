@model IEnumerable<SFCTest.Models.StationGroup>

@{
    ViewBag.Title = "Index";
}

<h2>Station</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.STATION_GROUP_CODE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPTION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.STATION_GROUP_CODE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPTION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDStationGroup }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDStationGroup }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDStationGroup })
        </td>
    </tr>
}

</table>
